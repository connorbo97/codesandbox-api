{
  "name": "@f/extend",
  "description": "Simple shallow extend implementation",
  "repository": "git://github.com/micro-js/extend.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach-obj": "^1.0.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
