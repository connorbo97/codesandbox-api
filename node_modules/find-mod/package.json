{
  "name": "find-mod",
  "description": "Find the closest directory containing node_modules.",
  "repository": "git://github.com/joshrtay/find-mod.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "mz": "^2.4.0"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "tape": "^4.2.2"
  }
}
